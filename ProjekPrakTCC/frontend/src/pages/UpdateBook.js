import React from 'react';
import UpdateBookForm from '../components/UpdateBookForm'

const UpdateBook = props => {
    return (
        <UpdateBookForm {...props} />
    );
}

export default UpdateBook;
